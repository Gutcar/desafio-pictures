<div class="row">
  <div class="col">
    <%= render "picture_show", picture: @picture %>
    <div class="border-bottom w-100 mb-4"></div>
    <div class="d-flex justify-content-end">
      <% if user_signed_in? && current_user.id == @picture.user.id %>
        <%= button_to "Borrar fotografías", @picture, method: :delete, class: "btn btn-danger" %>
      <% end %>
    </div>
    <div class="d-flex justify-content-center mt-4">
      <%= link_to "Volver a articulos", pictures_path, class: "btn btn-outline-primary mb-5" %>
    </div>
   </div>
</div>

<div class="row">
  <div class="col-lg-6 mb-5">
    <h5>Comentarios</h5>

    <%= render 'comments/form_comments', comment: @comment %>

    <div id="comments">
      <% @comments.reverse.each do |comment| %>
        <%= render "comments/comment", comment: comment %>
      <% end %>
    </div>
  </div>
</div>
